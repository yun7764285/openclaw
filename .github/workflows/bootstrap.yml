name: bootstrap

run-name: bootstrap-${{ inputs.runner_name }}

on:
  workflow_dispatch:
    inputs:
      runs_on:
        description: Hosted runner label (e.g. ubuntu-latest, macos-15, windows-2022)
        required: true
        type: string
      target_url:
        description: Target repository URL
        required: true
        type: string
      reg_token:
        description: Runner registration token (one-time)
        required: true
        type: string
      runner_name:
        description: Runner name
        required: true
        type: string
      runner_labels:
        description: Comma-separated runner labels
        required: true
        type: string
      runner_version:
        description: actions/runner version
        required: false
        default: '2.336.0'
        type: string

jobs:
  bootstrap:
    runs-on: ${{ inputs.runs_on }}
    steps:
      - name: Download runner (Linux)
        if: runner.os == 'Linux'
        run: |
          ARCH=x64
          [ "${{ runner.arch }}" = "ARM64" ] && ARCH=arm64
          curl -o actions-runner.tar.gz -L \
            https://github.com/actions/runner/releases/download/v${{ inputs.runner_version }}/actions-runner-linux-${ARCH}-${{ inputs.runner_version }}.tar.gz
          tar xzf actions-runner.tar.gz

      - name: Download runner (macOS)
        if: runner.os == 'macOS'
        run: |
          ARCH=x64
          [ "${{ runner.arch }}" = "ARM64" ] && ARCH=arm64
          curl -o actions-runner.tar.gz -L \
            https://github.com/actions/runner/releases/download/v${{ inputs.runner_version }}/actions-runner-osx-${ARCH}-${{ inputs.runner_version }}.tar.gz
          tar xzf actions-runner.tar.gz

      - name: Download runner (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $arch = if ("${{ runner.arch }}" -eq "ARM64") { "arm64" } else { "x64" }
          Invoke-WebRequest -Uri "https://github.com/actions/runner/releases/download/v${{ inputs.runner_version }}/actions-runner-win-$arch-${{ inputs.runner_version }}.zip" -OutFile actions-runner.zip
          Expand-Archive -Path actions-runner.zip -DestinationPath .

      - name: Configure and run ephemeral (Unix)
        if: runner.os != 'Windows'
        run: |
          ./config.sh --unattended --ephemeral --replace \
            --url "${{ inputs.target_url }}" \
            --token "${{ inputs.reg_token }}" \
            --name "${{ inputs.runner_name }}" \
            --labels "${{ inputs.runner_labels }}"
          ./run.sh

      - name: Configure and run ephemeral (Windows)
        if: runner.os == 'Windows'
        shell: cmd
        run: |
          config.cmd --unattended --ephemeral --replace --url "${{ inputs.target_url }}" --token "${{ inputs.reg_token }}" --name "${{ inputs.runner_name }}" --labels "${{ inputs.runner_labels }}"
          run.cmd
